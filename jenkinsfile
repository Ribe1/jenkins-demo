pipeline {
    agent any

    stages {


       stage( 'Linux Commands' ) { 
            steps {
                
                sh 'pwd'
                
                sh 'hostname'

                sh 'date'

                sh 'uptime'

                sh 'ls -lrt jenkins-demo'
            }
        }
     }
}
